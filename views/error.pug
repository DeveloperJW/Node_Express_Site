extends layout

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro
        div
            h1.animated.shake.slower= error.message
            h2.animated.fadeIn.slower= error.status
            pre= error.stack